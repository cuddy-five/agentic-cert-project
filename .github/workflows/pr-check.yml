name: pr-check

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  describe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show what this PR touches
        run: git fetch origin main --depth=1 && git diff --name-only origin/main...HEAD
